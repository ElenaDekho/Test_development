# Разработка тестов в Python

Учебный проект по теме **«Разработка тестов в Python»**.  
Содержит три задачи: unit-тесты, API-тесты и UI-тесты с использованием Selenium.


---

## 🧪 Задача 1: Unit-тесты

Написаны параметризованные unit-тесты с использованием `unittest` для трёх задач из модуля «Основы Python»:

1. **«Секретарь»** — работа с документами и полками.
2. **«Подсчитываем тёзок»** — поиск людей с одинаковыми именами на курсах.
3. **«Топ-3 популярных имён»** — статистика по именам преподавателей.

Все функции **возвращают результат**, а не печатают (`print` → `return`), что позволяет их протестировать.

---

## 🌐 Задача 2: Unit-тест API Яндекс.Диска

Реализованы unit-тесты для REST API Яндекс.Диска с использованием `unittest.mock`:

- Проверка успешного создания папки (статус `201` или `409`),
- Негативные сценарии: неверный токен (`401`), недопустимое имя папки (`404`).

> «В unit-test токен не используется (моки). При необходимости интеграционного теста — токен передаётся через переменную окружения YANDEX_TOKEN»

---

## 🖥️ Задача 3: UI-тест с Selenium

Написан UI-тест для формы авторизации Яндекса (`https://passport.yandex.ru/auth/`):

- Проверка загрузки страницы,
- Наличие поля ввода и кнопки «Войти»,
- Эмуляция ввода тестового номера для активации UI.

> 💡 Тест **не выполняет реальную авторизацию** (это невозможно без 2FA), а проверяет **готовность формы к взаимодействию** — что соответствует духу unit/UI-тестирования.

⚠️ Используется **локальный `chromedriver.exe`** (64-bit), загруженный вручную (включён бинарный файл для стабильной работы теста на Windows).
Альтернатива — использовать webdriver-manager, но он может вызывать ошибки WinError 193 на некоторых системах.

---

## ⚙️ Установка и запуск

1. Установите зависимости:
   
   pip install -r requirements.txt

2. Для запуска всех тестов:
   
   python -m unittest discover -s . -p "test_*.py" -v

3. Для запуска отдельных групп:
   
   python -m unittest discover -s tests -v
   python -m unittest discover -s api_tests -v
   python -m unittest discover -s selenium_tests -v
